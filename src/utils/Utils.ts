export const mongoose = require("../config/database");

export const Schema = mongoose.Schema;

export const ReportModel = require("../model/Report");

export const RoomModel = require("../model/Room");

export const UserModel = require("../model/User");
